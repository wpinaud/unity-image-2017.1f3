# unity-image-2017.1f3
Docker image containing Unity 2017.1f3 ready for building your project under Linux

To build it: docker build -t unity-image-2017.1f3 .

You might want, however, to push that image to a remote registry so all build nodes have access to the image.
